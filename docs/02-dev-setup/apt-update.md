# 시스템 업데이트

출처: PDF p.27–30

터미널: `Ctrl + Alt + T` 또는 바탕화면 우클릭 → Open in Terminal

```bash
sudo apt update
sudo apt upgrade   # Continue? → y
sudo reboot
```

재부팅 후 다음 문서([clone-build-bashrc.md](clone-build-bashrc.md))로 진행한다.
